#include <iostream>

using namespace std;

int
main() {
    bool isRaining = false;

    // set stream to print booleans as words instead of 0 and 1
    cout << boolalpha;
    cout << "isRaining = " << isRaining << endl;
    return 0;
}
